import * as React from 'react';
import classes from './buttons.module.scss';

const CheckOutBtn: React.FC = () => {
  return <button className={classes.btn}>proceed to checkout</button>;
};
export default CheckOutBtn;
